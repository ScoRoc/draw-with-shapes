const initialState = {
  red: 'red',
  orange: 'orange',
  yellow: 'yellow',
  green: 'green',
  blue: 'blue',
  purple: 'purple',
  white: 'white',
  black: 'black'
};

export default colors = (state = initialState, action) => {
  return state;
};
